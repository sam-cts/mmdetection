{   
    "name": "mmdetection",
    "dockerFile": "../docker/Dockerfile",
    "runArgs": ["--rm", "-it", "--gpus", "all"],
    "extensions": ["dbaeumer.vscode-eslint"],
    "mounts": [
        "source=/mnt/sda5/datasets,target=/datasets,type=bind,consistency=cached"
      ]
    
 }